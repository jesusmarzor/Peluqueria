version: v1.0
name: Semaphore
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Docker
    task:
      jobs:
        - name: Correr Test
          commands:
            - checkout
            - docker run -t -v `pwd`:/app/test jesusmarzor/peluqueria:latest