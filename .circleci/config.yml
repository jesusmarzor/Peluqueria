version: 2.1

jobs:
  docker:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout
      - run:
          name: Ejecutar tests
          command: docker run -t -v `pwd`:/app/test jesusmarzor/peluqueria

workflows:
  tests:
    jobs:
      - docker
